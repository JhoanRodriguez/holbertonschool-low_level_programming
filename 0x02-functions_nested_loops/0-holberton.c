#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "holberton.h"
/**
 * main - main block
 * Description: Get a random number and print the number
 * and if it is positive, negative, or zero
 * Return: 0
 */
int main(void)
{
	putchar (72);
	putchar (111);
	putchar (108);
	putchar (98);
	putchar (101);
	putchar (114);
	putchar (116);
	putchar (111);
	putchar (110);
	putchar ('\n');
	return (0);
}
