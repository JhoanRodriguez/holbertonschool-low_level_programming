#include "holberton.h"
/**
 * _isupper - Print alphabet in lowercase 10 times
 * Return: 0
 * @x: char type letter
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
